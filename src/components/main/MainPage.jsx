import React from 'react';
import styles from './MainPage.module.css';

const MainPage = () => {
  return <section className={styles.page}>{/* Content (будет в руте) */}</section>;
};

export default MainPage;
