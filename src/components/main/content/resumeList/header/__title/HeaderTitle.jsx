import React from 'react';
import styles from './HeaderTitle.module.css';

const HeaderTitle = () => {
  return <h1 className={styles.header__title}>Список резюме</h1>;
};

export default HeaderTitle;
